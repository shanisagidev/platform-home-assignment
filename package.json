{
  "name": "platform-home-assignment",
  "version": "1.0.0",
  "description": "Mock DSPM Portal for automation testing",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "test:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@types/node": "^20.19.42",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "install": "^0.13.0",
    "npx": "^10.2.2",
    "playwright": "^1.60.0"
  }
}
